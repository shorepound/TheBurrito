<%= form_with(model: [:admin, tortilla], local: true) do |f| %>
  <% if tortilla.errors.any? %>
    <div class="alert alert-danger">
      <ul>
        <% tortilla.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= f.label :name %>
    <%= f.text_field :name, class: "form-control" %>
  </div>

  <div>
    <%= f.submit class: "btn btn-primary" %>
    <%= link_to "Cancel", admin_tortillas_path, class: "btn btn-link" %>
  </div>
<% end %>
